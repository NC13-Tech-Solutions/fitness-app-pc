/**
 * Represents a structure for organizing day, weeks, month, and year data.
 * @type {Day: number, Weeks: number[][], Month: string, Year: string}
 */

export interface DayWeeksMonthYear {
  Day: number;
  Weeks: number[][];
  Month: string;
  Year: number;
}
