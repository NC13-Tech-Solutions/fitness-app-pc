/**
 * Represents date information including the day, month, and year.
 * @type { day: number, month: string, year: number } */

export interface DateData {
  day: number;
  month: string;
  year: number;
}
